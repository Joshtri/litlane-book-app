<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title %></title>

    <meta name="description" content="<%= locals.description %>">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

</head>
<body>

    <!-- start navbar header -->
    <%-include('partials/header'); %>
    <!-- end navbar header -->

    <div class="container">
        <div class="d-flex justify-content-between flex-wrap flex-md nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
          <h4 class="h4">Editing: <%= book.title_book %></h4>

          <div class="btn-toolbar mb-2 mb-md 0">
            <div class="btn-group me-2">
              <button class="btn btn-sm btn-outline-secondary">?</button>
            </div>
          </div>
          

        </div>
        
        <div class="col py-3">
          <div class="row">
            <div class="col">
              <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                  <li class="breadcrumb-item"><a href="/mdERQU0pnVpHd08ifQ/adm/dashboardUser">Dashboard</a></li>
                  <li class="breadcrumb-item active"><%= book.firstName %> <%= book.lastName %></li>
                </ol>
              </nav>
            </div>
            <div class="col text-end fw-lighter">
              <!-- <b>Last Updated: <%= new Date(book.updatedAt).toUTCString()  %></b>
              <b>UserId:</b> <%= book._id %> -->
            </div>
          </div>
        
        </div>
        
        
        <form action="/post_edit_book/<%= book._id %>?_method=PUT" method="POST">
        
          <div class="row form-group mb-4">
            <div class="col">
              <label for="firstName">Title Book</label>
              <input type="text" class="form-control" id="firstName" name="title_book" value="<%= book.title_book %>" placeholder="Title Book" required>
            </div>
        
            <div class="col">
              <label for="lastName">Author</label>
              <input type="text" class="form-control" id="lastName" name="author" value="<%= book.author %>" placeholder="Last Name" required>
            </div>
          </div>
        
          <div class="row form-group mb-4">
            <div class="col">
              <label for="tel">Publisher</label>
              <input type="text" class="form-control" id="tel" name="publisher" value="<%= book.publisher %>" placeholder="Telephone" required>
            </div>
        
            <div class="col">
              <label for="email">Publication Year</label>
              <!-- sementara pake input dengan type date -->
              <input type="date" class="form-control" id="email" name="email" value="<%= book.publication_year %>" placeholder="Email" required>
            </div>
          </div>
        
          <div class="form-group mb-4">
            <label for="email">Number of Page</label>
            <input type="text" class="form-control" id="email" name="number_of_page" value="<%= book.number_of_page %>" placeholder="Email" required>
          </div>
        
          <div class="form-group mb-4">
            <button type="submit" class="btn btn-primary">Update</button>
        
            <button type="button" class="btn btn-danger" data-bs-toggle="modal" id="deleteButton" data-bs-target="#deleteModal">Delete</button>
            <a href="/mdERQU0pnVpHd08ifQ/adm/data_book"  class="btn btn-warning">Back</a>
        
          </div>
        
        </form>
        
        
        
        <div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <div class="modal-title">You are about to remove a book record.</div>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                <p>
                  This will remove the book record of <b class="fw-bold"><%= book.title_book %></b><br/>
                  Are you sure?
                </p>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <form action="/mdERQU0pnVpHd08ifQ/adm/post_delete_book/<%= book._id %>?_method=DELETE" method="POST" class="position-relative">
                  <button type="submit" class="btn btn-primary">Yes, Remove Book</button>
                </form>
              </div>
            </div>
          </div>
        </div>
    </div>

    <script src="/assets/vendor/bootstrap/js/bootstrap.bundle.js"></script>
</body>
</html>